name: Validate static prerender

on:
  push:
    branches: ["refactor/static-prerender"]
  pull_request:
    branches: ["main", "expansion/lavoro-istruzione-abitare"]

permissions:
  contents: read

concurrency:
  group: static-preview-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install browser tooling
        run: |
          python -m pip install --upgrade pip
          python -m pip install playwright requests
          python -m playwright install --with-deps chromium

      - name: Discover OpenBDAP budget sources
        run: python scripts/discover_openbdap_budget_sources.py

      - name: Probe Tuscany budget documents
        run: python scripts/probe_openbdap_documents.py

      - name: Upload OpenBDAP audit snapshots
        uses: actions/upload-artifact@v4
        with:
          name: openbdap-budget-discovery
          path: |
            data/source-snapshots/openbdap-budget-discovery.json
            data/source-snapshots/openbdap-budget-documents.json
          retention-days: 7

      - name: Build pre-rendered site
        run: python scripts/build_static_safe.py

      - name: Run regression tests
        run: python scripts/test_static.py

      - name: Run v1.5.0 regional indicator checks
        run: python scripts/test_release_v150.py

      - name: Upload private build artifact
        uses: actions/upload-artifact@v4
        with:
          name: osservatorio-versilia-static-preview
          path: dist
          retention-days: 7

# Deliberately no deploy-pages job: this workflow validates PRs but cannot publish the site.
