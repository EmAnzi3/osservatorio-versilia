# v1.35.0 — Biometria del comune — draft

Status: **DRAFT / non merge-ready**

## Implemented in this draft

- canonical source scaffold for the seven Versilia municipalities;
- municipal surface area;
- percentage distribution across the eight ISTAT altitude bands;
- derived thresholds `from300Pct` and `below600Pct`;
- materializer with optional density derivation from a canonical resident-population JSON;
- source integrity tests.

## Local validation completed

- JSON parse;
- exactly seven municipalities;
- exactly eight ISTAT altitude bands per municipality;
- every percentage in the 0..100 range;
- band totals within ±0.2 percentage points of 100 to tolerate source rounding;
- derived threshold checks;
- explicit guard against fabricated min/mean/max altitude values;
- Python syntax and unit tests: 5/5 passing;
- self-contained HTML review artifact generated outside the repository.

## Open data / release gates

1. Verify and ingest minimum, mean and maximum altitude from the canonical ISTAT file `Altimetria_Comuni-al-31_12_2021.xlsx`.
2. Resolve the canonical resident population already used by Osservatorio Versilia and derive density from `population / surfaceKm2`.
3. Integrate the approved UI into the production municipal/territorial runtime after visual review of the artifact.
4. Run repository-wide `preflight --quick` from a runtime with a working GitHub checkout/network path.

No merge or release is authorized by this draft.
